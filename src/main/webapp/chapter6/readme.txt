Goal:
	* master-detail CRUD page handling in MVC
		* ListModel
		* @Listen("onClick = #addTodo; onOK = #todoSubject")
	* master-detail CRUD page handling in MVVM
		* using ListModelList in MVVM 
		* dynamic notify change
		* validator